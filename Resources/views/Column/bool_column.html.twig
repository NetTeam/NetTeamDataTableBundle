{% extends "NetTeamDataTableBundle:Column:column.html.twig" %}

{% block columnValue %}
    {% if record.value %}
       {{ text_true|trans({}, translation_domain|default('messages')) }}
    {% else %}
        {{ text_false|trans({}, translation_domain|default('messages')) }}
    {% endif %}
{% endblock %}
